<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- WorkTracker dark palette -->
    <SolidColorBrush x:Key="WtWindowBrush" Color="#0B1020"/>
    <SolidColorBrush x:Key="WtPanelBrush" Color="#121A2B"/>
    <SolidColorBrush x:Key="WtPanelAltBrush" Color="#172033"/>
    <SolidColorBrush x:Key="WtInputBrush" Color="#0D1424"/>
    <SolidColorBrush x:Key="WtHoverBrush" Color="#17243A"/>
    <SolidColorBrush x:Key="WtSelectedBrush" Color="#223556"/>
    <SolidColorBrush x:Key="WtPressedBrush" Color="#1D2B43"/>
    <SolidColorBrush x:Key="WtBorderBrush" Color="#2A3854"/>
    <SolidColorBrush x:Key="WtBorderStrongBrush" Color="#3A4C6E"/>
    <SolidColorBrush x:Key="WtTextBrush" Color="#E6EDF7"/>
    <SolidColorBrush x:Key="WtMutedBrush" Color="#92A0B5"/>
    <SolidColorBrush x:Key="WtPrimaryBrush" Color="#5B8FFF"/>
    <SolidColorBrush x:Key="WtPrimaryHoverBrush" Color="#527FE0"/>
    <SolidColorBrush x:Key="WtPrimaryPressedBrush" Color="#416BC5"/>
    <SolidColorBrush x:Key="WtPrimarySoftBrush" Color="#172A4F"/>
    <SolidColorBrush x:Key="WtSuccessBrush" Color="#56D364"/>
    <SolidColorBrush x:Key="WtWarningBrush" Color="#E3B341"/>
    <SolidColorBrush x:Key="WtDangerBrush" Color="#FF7B72"/>
    <SolidColorBrush x:Key="WtSelectionBrush" Color="#315CA8"/>
    <SolidColorBrush x:Key="WtScrollThumbBrush" Color="#465A7B"/>
    <SolidColorBrush x:Key="WtScrollThumbHoverBrush" Color="#5C739A"/>

    <CornerRadius x:Key="WtCardRadius">14</CornerRadius>
    <Thickness x:Key="WtControlPadding">11,6</Thickness>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WtWindowBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12.5"/>
    </Style>

    <Style TargetType="UserControl">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Padding" Value="{StaticResource WtControlPadding}"/>
        <Setter Property="Margin" Value="3,0"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelAltBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderStrongBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonSurface"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
                <Setter Property="BorderBrush" Value="#405575"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource WtPressedBrush}"/>
                <Setter Property="BorderBrush" Value="#4A6083"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.42"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WtPrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{StaticResource WtPrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtPrimaryHoverBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource WtPrimaryPressedBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryPressedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WtDangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="#2B1820"/>
        <Setter Property="Foreground" Value="#FFB4AE"/>
        <Setter Property="BorderBrush" Value="#60303A"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#371D26"/>
                <Setter Property="BorderBrush" Value="#75404B"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#42222D"/>
                <Setter Property="BorderBrush" Value="#87505B"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Padding" Value="9,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource WtSelectionBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Padding" Value="9,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WtComboHitArea" TargetType="ToggleButton">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Native Windows ComboBox templates can keep a light selection surface even when
         Background is set. This custom template owns the complete visual surface. -->
    <Style TargetType="ComboBox">
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MaxDropDownHeight" Value="320"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="Shell"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Stretch"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  IsHitTestVisible="False"/>
                                <Path Grid.Column="1"
                                      Width="8" Height="5"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Fill="{StaticResource WtMutedBrush}"
                                      Data="M 0 0 L 8 0 L 4 5 Z"/>
                            </Grid>
                        </Border>
                        <ToggleButton Style="{StaticResource WtComboHitArea}"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               PopupAnimation="Fade"
                               Focusable="False">
                            <Border MinWidth="{Binding ActualWidth, ElementName=Shell}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Margin="0,4,0,0"
                                    Padding="3"
                                    Background="{StaticResource WtPanelBrush}"
                                    BorderBrush="{StaticResource WtBorderStrongBrush}"
                                    BorderThickness="1"
                                    CornerRadius="7">
                                <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Shell" Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Shell" Property="Background" Value="{StaticResource WtPanelAltBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Shell" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="9,6"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
                <Setter Property="Foreground" Value="#EAF1FF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Grid Width="17" Height="17" Margin="0,0,7,0">
                            <Border x:Name="CheckSurface"
                                    Background="{StaticResource WtInputBrush}"
                                    BorderBrush="{StaticResource WtBorderStrongBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"/>
                            <Path x:Name="CheckMark"
                                  Visibility="Collapsed"
                                  Stroke="#DCE8FF"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Data="M 3 8 L 7 12 L 14 4"/>
                            <Border x:Name="IndeterminateMark"
                                    Visibility="Collapsed"
                                    Width="9" Height="2"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="#DCE8FF"
                                    CornerRadius="1"/>
                        </Grid>
                        <ContentPresenter VerticalAlignment="Center"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckSurface" Property="Background" Value="{StaticResource WtPrimarySoftBrush}"/>
                            <Setter TargetName="CheckSurface" Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="CheckSurface" Property="Background" Value="{StaticResource WtPrimarySoftBrush}"/>
                            <Setter TargetName="CheckSurface" Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
                            <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CheckSurface" Property="BorderBrush" Value="#54709A"/>
                            <Setter TargetName="CheckSurface" Property="Background" Value="#121D30"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="WtCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="Padding" Value="13"/>
        <Setter Property="CornerRadius" Value="{StaticResource WtCardRadius}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="WtMetricCard" TargetType="Border" BasedOn="{StaticResource WtCard}">
        <Setter Property="Margin" Value="6,0"/>
    </Style>

    <Style x:Key="WtStatusBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WtPrimarySoftBrush}"/>
        <Setter Property="BorderBrush" Value="#31568F"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="WtMutedText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtMutedBrush}"/>
        <Setter Property="FontSize" Value="11.5"/>
    </Style>

    <Style x:Key="WtSectionLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtMutedBrush}"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="WtMetricValue" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,5,0,0"/>
    </Style>

    <Style x:Key="WtCurrentActivity" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="FontSize" Value="15.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,6,0,3"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <!-- Own the TabControl content surface so Windows' light theme cannot leak through. -->
    <Style TargetType="TabControl">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource WtWindowBrush}">
                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Margin="0,0,0,-1" Panel.ZIndex="1"/>
                        </Border>
                        <Border Grid.Row="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8,0,8,8">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Margin="0"
                                              ContentSource="SelectedContent"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource WtMutedBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtWindowBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="Padding" Value="13,7"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="HeaderBorder"
                            Margin="0,0,3,0"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="7,7,0,0">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource WtHoverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource WtPrimarySoftBrush}"/>
                            <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
                            <Setter Property="Foreground" Value="#C9DAFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListView">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
    </Style>

    <Style TargetType="ListViewItem">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="3"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="5"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Foreground" Value="#AEB9CA"/>
        <Setter Property="Background" Value="#111A2C"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,7"/>
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#202C42"/>
        <Setter Property="RowBackground" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="AlternatingRowBackground" Value="#10192B"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Foreground" Value="#AEB9CA"/>
        <Setter Property="Background" Value="#111A2C"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,7"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="5,4"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WtScrollThumb" TargetType="Thumb">
        <Setter Property="MinWidth" Value="9"/>
        <Setter Property="MinHeight" Value="9"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder" Margin="2" Background="{StaticResource WtScrollThumbBrush}" CornerRadius="5"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource WtScrollThumbHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="WtScrollTrackButton" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="WtVerticalScrollBarTemplate" TargetType="ScrollBar">
        <Border Width="13" Background="{StaticResource WtInputBrush}">
            <Track x:Name="PART_Track" IsDirectionReversed="True" Orientation="Vertical" Focusable="False">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource WtScrollTrackButton}" Command="ScrollBar.PageUpCommand"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource WtScrollThumb}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource WtScrollTrackButton}" Command="ScrollBar.PageDownCommand"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="WtHorizontalScrollBarTemplate" TargetType="ScrollBar">
        <Border Height="13" Background="{StaticResource WtInputBrush}">
            <Track x:Name="PART_Track" IsDirectionReversed="False" Orientation="Horizontal" Focusable="False">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource WtScrollTrackButton}" Command="ScrollBar.PageLeftCommand"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource WtScrollThumb}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource WtScrollTrackButton}" Command="ScrollBar.PageRightCommand"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Border>
    </ControlTemplate>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource WtInputBrush}"/>
        <Setter Property="Template" Value="{StaticResource WtVerticalScrollBarTemplate}"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource WtHorizontalScrollBarTemplate}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="0,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelAltBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderStrongBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
    </Style>
</ResourceDictionary>
