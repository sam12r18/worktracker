<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- WorkTracker dark palette -->
    <SolidColorBrush x:Key="WtWindowBrush" Color="#0B1020"/>
    <SolidColorBrush x:Key="WtPanelBrush" Color="#121A2B"/>
    <SolidColorBrush x:Key="WtPanelAltBrush" Color="#172033"/>
    <SolidColorBrush x:Key="WtInputBrush" Color="#0D1424"/>
    <SolidColorBrush x:Key="WtHoverBrush" Color="#1C2941"/>
    <SolidColorBrush x:Key="WtSelectedBrush" Color="#263A65"/>
    <SolidColorBrush x:Key="WtBorderBrush" Color="#2A3854"/>
    <SolidColorBrush x:Key="WtBorderStrongBrush" Color="#3A4C6E"/>
    <SolidColorBrush x:Key="WtTextBrush" Color="#E6EDF7"/>
    <SolidColorBrush x:Key="WtMutedBrush" Color="#92A0B5"/>
    <SolidColorBrush x:Key="WtPrimaryBrush" Color="#5B8FFF"/>
    <SolidColorBrush x:Key="WtPrimaryHoverBrush" Color="#6EA8FE"/>
    <SolidColorBrush x:Key="WtPrimarySoftBrush" Color="#172A4F"/>
    <SolidColorBrush x:Key="WtSuccessBrush" Color="#56D364"/>
    <SolidColorBrush x:Key="WtWarningBrush" Color="#E3B341"/>
    <SolidColorBrush x:Key="WtDangerBrush" Color="#FF7B72"/>
    <SolidColorBrush x:Key="WtSelectionBrush" Color="#315CA8"/>

    <CornerRadius x:Key="WtCardRadius">14</CornerRadius>
    <Thickness x:Key="WtControlPadding">11,6</Thickness>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WtWindowBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12.5"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Padding" Value="{StaticResource WtControlPadding}"/>
        <Setter Property="Margin" Value="3,0"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelAltBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderStrongBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
                <Setter Property="BorderBrush" Value="#58719A"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WtPrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{StaticResource WtPrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtPrimaryHoverBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryHoverBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Padding" Value="9,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource WtSelectionBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Padding" Value="9,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource WtPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="Padding" Value="8,5"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
    </Style>

    <Style x:Key="WtCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="Padding" Value="13"/>
        <Setter Property="CornerRadius" Value="{StaticResource WtCardRadius}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="WtMetricCard" TargetType="Border" BasedOn="{StaticResource WtCard}">
        <Setter Property="Margin" Value="6,0"/>
    </Style>

    <Style x:Key="WtStatusBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WtPrimarySoftBrush}"/>
        <Setter Property="BorderBrush" Value="#31568F"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="WtMutedText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtMutedBrush}"/>
        <Setter Property="FontSize" Value="11.5"/>
    </Style>

    <Style x:Key="WtSectionLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtMutedBrush}"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="WtMetricValue" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,5,0,0"/>
    </Style>

    <Style x:Key="WtCurrentActivity" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="FontSize" Value="15.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,6,0,3"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource WtMutedBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtWindowBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="Padding" Value="13,7"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="#B7CEFF"/>
                <Setter Property="Background" Value="{StaticResource WtPrimarySoftBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListView">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
    </Style>

    <Style TargetType="ListViewItem">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="3"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="5"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Foreground" Value="#AEB9CA"/>
        <Setter Property="Background" Value="#111A2C"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,7"/>
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#202C42"/>
        <Setter Property="RowBackground" Value="{StaticResource WtPanelBrush}"/>
        <Setter Property="AlternatingRowBackground" Value="#10192B"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Foreground" Value="#AEB9CA"/>
        <Setter Property="Background" Value="#111A2C"/>
        <Setter Property="BorderBrush" Value="{StaticResource WtBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,7"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="5,4"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Foreground" Value="{StaticResource WtTextBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource WtHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource WtSelectedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
